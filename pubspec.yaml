name: cbl_dart_repo
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  cbd:
    path: tool
  daco: ^0.4.0
  melos: ^6.3.2
  path: ^1.9.1

dependency_overrides:
  cbl:
    path: packages/cbl

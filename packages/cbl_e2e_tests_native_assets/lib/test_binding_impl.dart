// This is just a stub to allow the tests to pass checking by the analyzer.
// The packages which actually run the tests must provide a real implementation
// where they setup up the test bindings.
void setupTestBinding() {
  throw UnimplementedError();
}
